export { handleKeyDown } from '@/lib/asteroid/_comp/Game/handleKeyDown';

// Default placeholder so Next won't treat this as an invalid page
export default function _PagePlaceholder() {
  return null;
}
