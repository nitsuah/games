export { handleHealthDepletion } from '@/lib/asteroid/_comp/Game/handleHealthDepletion';

// Placeholder default export so Next doesn't treat this helper as a page without a component.
export default function _PagePlaceholder() {
  return null;
}
